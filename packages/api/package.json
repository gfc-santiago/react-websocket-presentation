{
  "name": "api",
  "version": "1.0.0",
  "description": "Web-socket server for page switching",
  "main": "./src/index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "dev": "set DEBUG=socket* && nodemon ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
